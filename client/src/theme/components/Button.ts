const baseStyle = {
  fontFamily: "button",
  fontWeight: "button",
  textTransform: "capitalize",
  letterSpacing: 2,
};

const defaultProps = {
  colorScheme: "primary",
};

export default {
  baseStyle,
  defaultProps,
};
